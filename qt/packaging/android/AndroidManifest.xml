<?xml version="1.0"?>
<!-- xournal-qt: the Android manifest. Qt's template (Qt 6.11, src/android/templates) with the app's own settings:
     "All files access" for libraries in the shared storage (below), a resizable activity for
     split screen, pop-up windows, DeX and the Fold's two screens, and an optional stylus. The activity is Qt's with
     the handling of files from other apps (src/org/xournalqt/app/XournalActivity.java): "Open with" and the share
     sheet for PDFs, Xournal++ files, Markdown and text files and images. The %%INSERT_...%% markers are filled in by
     androiddeployqt from the target's QT_ANDROID_* properties (qt/cmake/XqtAndroid.cmake). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.xournalqt.app"
    android:installLocation="auto"
    android:versionCode="-- %%INSERT_VERSION_CODE%% --"
    android:versionName="-- %%INSERT_VERSION_NAME%% --">
    <!-- Only these permissions: without the marker of Qt's template for permissions, androiddeployqt adds none of the
         ones its modules ask for by default (INTERNET for Qt Network, WRITE_EXTERNAL_STORAGE); Qt Network is there for
         the local socket. "All files access" is asked for only when the user opens a folder of the shared storage as
         a library (with an explanation first, see qt/docs/android.md): libraries in folders that sync apps
         (Syncthing, FolderSync) keep, read by their paths like on the desktop. Before Android 11 the storage
         permission does the same. (Google Play restricts MANAGE_EXTERNAL_STORAGE; fine for sideloading and F-Droid.) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <!-- %%INSERT_FEATURES -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen.multitouch" android:required="false" />
    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:hardwareAccelerated="true"
        android:label="-- %%INSERT_APP_NAME%% --"
        android:icon="-- %%INSERT_APP_ICON%% --"
        android:enableOnBackInvokedCallback="false"
        android:allowBackup="true"
        android:fullBackupOnly="false"
        android:requestLegacyExternalStorage="true"
        android:resizeableActivity="true">
        <activity
            android:name="org.xournalqt.app.XournalActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:label="-- %%INSERT_APP_NAME%% --"
            android:launchMode="singleTask"
            android:screenOrientation="unspecified"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- "Open with": a copy goes into the library (folder "Opened") and opens (AppController::receiveFiles).
                 One task (singleTask): a file handed over while the app runs opens in it as a tab. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/x-xopp" />
                <data android:mimeType="application/x-xojpp" />
                <data android:mimeType="text/markdown" />
                <data android:mimeType="text/x-markdown" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <!-- .xopp and .xoj files have no MIME type most apps know: file managers hand them over as a byte stream
                 (a .xopp is gzip) or with their name only -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/gzip" />
                <data android:mimeType="application/x-gzip" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.xopp" />
                <data android:pathPattern=".*\\..*\\.xopp" />
                <data android:pathPattern=".*\\.xoj" />
                <data android:pathPattern=".*\\.md" />
            </intent-filter>
            <!-- The share sheet: one file or several -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/x-xopp" />
                <data android:mimeType="application/x-xojpp" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/gzip" />
                <data android:mimeType="text/markdown" />
                <data android:mimeType="text/x-markdown" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="-- %%INSERT_APP_LIB_NAME%% --" />

            <meta-data
                android:name="android.app.arguments"
                android:value="-- %%INSERT_APP_ARGUMENTS%% --" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.qtprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/qtprovider_paths"/>
        </provider>
    </application>
</manifest>
